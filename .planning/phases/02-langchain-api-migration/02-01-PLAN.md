---
phase: 02-langchain-api-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/tests/unit/test_imports.py, backend/llm/client.py]
autonomous: true
requirements: [API-01, API-02]
user_setup: []

must_haves:
  truths:
    - "All imports use langchain_core.* namespace"
    - "LLM client uses standard LangChain interfaces where applicable"
    - "No deprecated langchain.schema imports"
    - "Tests verify import correctness"
  artifacts:
    - path: "backend/tests/unit/test_imports.py"
      provides: "Import verification tests"
      exports: ["test_langchain_core_imports"]
    - path: "backend/llm/client.py"
      provides: "LLM client with LangChain compatibility"
      exports: ["LLMClient", "LLMResponse", "Message"]
  key_links:
    - from: "backend/llm/client.py"
      to: "langchain_core.messages"
      via: "import statement"
      pattern: "from langchain_core\\.messages import"
---

<objective>
Verify and migrate imports to langchain_core.* namespace, evaluate langchain-dashscope integration
</objective>

<execution_context>
@/Users/huahao/Desktop/code/claudecode/talk2code/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-langchain-api-migration/02-RESEARCH.md
@backend/llm/client.py
@backend/agents/state.py
@backend/agents/nodes.py
@backend/agents/workflow.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create import verification test file</name>
  <files>backend/tests/unit/test_imports.py</files>
  <read_first>
    - backend/tests/conftest.py (test fixtures pattern)
    - backend/tests/unit/test_llm_client.py (existing test structure)
    - backend/agents/state.py (current imports)
    - backend/agents/workflow.py (current imports)
  </read_first>
  <action>
    Create backend/tests/unit/test_imports.py with pytest tests that verify:
    1. All langchain imports use langchain_core.* namespace
    2. No deprecated langchain.schema imports
    3. Key types are importable: HumanMessage, SystemMessage, AIMessage, ChatPromptTemplate, StateGraph, END
    
    Test structure:
    - test_langchain_core_imports(): Verify core types can be imported
    - test_no_deprecated_imports(): Scan source files for deprecated patterns
    - test_project_imports_clean(): Verify project files use correct imports
  </action>
  <verify>
    <automated>pytest backend/tests/unit/test_imports.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/unit/test_imports.py exists with at least 3 test functions
    - pytest backend/tests/unit/test_imports.py -x passes
    - test_no_deprecated_imports scans: backend/llm/client.py, backend/agents/*.py
    - Test uses grep-style checking for 'from langchain.schema' pattern
  </acceptance_criteria>
  <done>
    Import verification test file created and all tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit llm/client.py for LangChain integration opportunity</name>
  <files>backend/llm/client.py</files>
  <read_first>
    - backend/llm/client.py (current implementation)
    - backend/config.py (DASHSCOPE configuration)
    - .planning/phases/02-langchain-api-migration/02-RESEARCH.md (research on langchain-dashscope)
  </read_first>
  <action>
    Review llm/client.py to determine if langchain-dashscope integration is beneficial.
    
    Current state: Uses direct requests to DashScope REST API with custom retry logic.
    
    Research conclusion from 02-RESEARCH.md: "保持现状，自定义客户端已满足需求且更可控"
    (Keep current implementation - custom client is sufficient and more controllable)
    
    Decision: Do NOT migrate llm/client.py to langchain-dashscope. The custom implementation:
    - Has better retry logic with exponential backoff
    - Supports stream/non-stream modes
    - Has session memory management
    - Is more controllable than langchain-dashscope wrapper
    
    Add a comment at the top of llm/client.py explaining why we use direct API:
    ```python
    # Note: Using direct DashScope REST API instead of langchain-dashscope
    # for better control over retry logic, streaming, and timeout handling.
    # See: .planning/phases/02-langchain-api-migration/02-RESEARCH.md
    ```
  </action>
  <verify>
    <automated>grep -n "direct DashScope" backend/llm/client.py</automated>
  </verify>
  <acceptance_criteria>
    - backend/llm/client.py has a comment explaining the direct API decision
    - Comment references 02-RESEARCH.md
    - No functional changes to LLMClient class (keep existing implementation)
  </acceptance_criteria>
  <done>
    llm/client.py retains custom implementation with explanatory comment added
  </done>
</task>

<task type="auto">
  <name>Task 3: Add langchain_core type hints to llm/client.py</name>
  <files>backend/llm/client.py</files>
  <read_first>
    - backend/llm/client.py (current implementation)
  </read_first>
  <action>
    Enhance llm/client.py with langchain_core types for better integration:
    
    1. Add imports at the top:
       ```python
       from langchain_core.messages import HumanMessage, AIMessage, SystemMessage, BaseMessage
       ```
    
    2. Create conversion helper function:
       ```python
       def to_langchain_message(msg: Message) -> BaseMessage:
           """Convert internal Message to langchain_core BaseMessage"""
           if msg.role == 'user':
               return HumanMessage(content=msg.content)
           elif msg.role == 'assistant':
               return AIMessage(content=msg.content)
           else:
               return SystemMessage(content=msg.content)
       ```
    
    3. Update load_memory method to use langchain_core types internally
    
    This enables future integration while keeping the custom API client.
  </action>
  <verify>
    <automated>python -c "from backend.llm.client import to_langchain_message, HumanMessage; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/llm/client.py imports HumanMessage, AIMessage, SystemMessage, BaseMessage from langchain_core.messages
    - to_langchain_message function exists and is exportable
    - Function converts Message(role='user') -> HumanMessage
    - Function converts Message(role='assistant') -> AIMessage
    - Function converts Message(role='system') -> SystemMessage
  </acceptance_criteria>
  <done>
    langchain_core types integrated into llm/client.py for future compatibility
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM Client → DashScope API | External API calls with API key authentication |
| Import boundaries | Ensure no untrusted code execution via imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | llm/client.py | mitigate | API key stored in environment variable, never logged |
| T-02-02 | Tampering | Import validation | mitigate | test_imports.py verifies no deprecated/compromised imports |
| T-02-03 | Information Disclosure | langchain_core integration | accept | Only using message types, no additional data exposure |
</threat_model>

<verification>
- pytest backend/tests/unit/test_imports.py -x passes
- grep "from langchain.schema" backend/*.py backend/**/*.py returns nothing
- grep "from langchain_core" backend/agents/*.py backend/llm/*.py returns results
</verification>

<success_criteria>
1. Import verification tests created and passing
2. No deprecated langchain.schema imports in codebase
3. llm/client.py has langchain_core type compatibility
4. Decision documented for keeping custom DashScope client
</success_criteria>

<output>
After completion, create .planning/phases/02-langchain-api-migration/02-01-SUMMARY.md
</output>
