---
phase: 03-test-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/tests/unit/test_imports.py, backend/tests/unit/test_llm_client.py, backend/tests/integration/test_workflow.py]
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "All unit tests pass when running pytest"
    - "LangChain imports use langchain_core.* namespace"
    - "LangGraph workflow executes without errors"
  artifacts:
    - path: "backend/tests/unit/test_imports.py"
      provides: "LangChain core import verification"
      exports: ["TestLangChainCoreImports", "TestNoDeprecatedImports"]
    - path: "backend/tests/unit/test_llm_client.py"
      provides: "LLM client unit tests"
      exports: ["TestLLMClientInit", "TestLLMClientMemory", "TestLLMClientChat"]
    - path: "backend/tests/integration/test_workflow.py"
      provides: "LangGraph workflow integration tests"
      exports: ["TestWorkflowCreation", "TestWorkflowInvocation"]
  key_links:
    - from: "backend/tests/unit/test_imports.py"
      to: "langchain_core.messages"
      via: "import statement"
      pattern: "from langchain_core.messages import"
    - from: "backend/tests/integration/test_workflow.py"
      to: "backend/agents/workflow.py"
      via: "workflow invoke"
      pattern: "workflow\\.invoke\\("
---

<objective>
Run existing unit and integration tests to verify LangChain 1.x migration is complete and all tests pass.

Purpose: Validate TEST-01, TEST-02, TEST-03 requirements by executing the test suite that was created in Phase 2.
Output: Test execution report confirming all tests pass with Python 3.11.11 environment.
</objective>

<execution_context>
@/Users/huahao/Desktop/code/claudecode/talk2code/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-langchain-api-migration/02-RESEARCH.md

# Key test files to verify
@backend/tests/unit/test_imports.py
@backend/tests/unit/test_llm_client.py
@backend/tests/integration/test_workflow.py

# Source files under test
@backend/llm/client.py
@backend/agents/workflow.py
@backend/agents/nodes.py
@backend/agents/state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run LangChain import verification tests</name>
  <read_first>
    - backend/tests/unit/test_imports.py (test file to execute)
    - backend/llm/client.py (verifies langchain_core imports)
    - backend/prompts.py (verifies ChatPromptTemplate usage)
  </read_first>
  <action>
    Run the import verification tests to confirm all LangChain imports use the correct langchain_core.* namespace.

    Command: cd /Users/huahao/Desktop/code/claudecode/talk2code/backend && python -m pytest tests/unit/test_imports.py -v

    Expected outcomes:
    - TestLangChainCoreImports tests pass (HumanMessage, SystemMessage, AIMessage, ChatPromptTemplate imports)
    - TestNoDeprecatedImports tests pass (no 'from langchain.schema' imports found)
    - TestProjectImportsClean tests pass (all project imports work correctly)

    If any test fails, examine the error output and identify which file has incorrect imports.
  </action>
  <acceptance_criteria>
    - pytest command exits with code 0
    - Output contains "passed" with no "failed" or "error"
    - All TestLangChainCoreImports tests pass (7 tests)
    - TestNoDeprecatedImports tests pass (2 tests)
    - TestProjectImportsClean tests pass (8 tests)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huahao/Desktop/code/claudecode/talk2code/backend && python -m pytest tests/unit/test_imports.py -v</automated>
  </verify>
  <done>
    Import verification tests pass, confirming all langchain_core.* imports are correct and no deprecated imports exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run LLM client unit tests</name>
  <read_first>
    - backend/tests/unit/test_llm_client.py (test file to execute)
    - backend/llm/client.py (source file under test)
  </read_first>
  <action>
    Run the LLM client unit tests to verify the DashScope API client integration with langchain_core types works correctly.

    Command: cd /Users/huahao/Desktop/code/claudecode/talk2code/backend && python -m pytest tests/unit/test_llm_client.py -v

    Expected outcomes:
    - TestMessage tests pass (message creation, timestamp format)
    - TestLLMResponse tests pass (response creation, error handling)
    - TestLLMClientInit tests pass (initialization with API key, default/custom params)
    - TestLLMClientMemory tests pass (clear, load, get memory)
    - TestLLMClientChat tests pass (chat success, system prompt, memory, error handling)
    - TestLLMClientStream tests pass (streaming chat)
    - TestGetClient tests pass (singleton pattern)
    - TestLLMClientRetry tests pass (retry logic)

    These tests use mocking, so they don't require actual API keys.
  </action>
  <acceptance_criteria>
    - pytest command exits with code 0
    - Output contains at least 15 tests passed
    - No test failures or errors
    - TestMessage: 2 tests pass
    - TestLLMResponse: 3 tests pass
    - TestLLMClientInit: 6 tests pass
    - TestLLMClientMemory: 4 tests pass
    - TestLLMClientChat: 3 tests pass
    - TestLLMClientRetry: 2 tests pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huahao/Desktop/code/claudecode/talk2code/backend && python -m pytest tests/unit/test_llm_client.py -v</automated>
  </verify>
  <done>
    LLM client unit tests pass, confirming the DashScope API client works correctly with mocked responses.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run LangGraph workflow integration tests</name>
  <read_first>
    - backend/tests/integration/test_workflow.py (test file to execute)
    - backend/agents/workflow.py (source file under test)
    - backend/agents/state.py (AgentState definition)
  </read_first>
  <action>
    Run the LangGraph workflow integration tests to verify the StateGraph workflow executes correctly with LangChain 1.x.

    Command: cd /Users/huahao/Desktop/code/claudecode/talk2code/backend && python -m pytest tests/integration/test_workflow.py -v

    Expected outcomes:
    - TestWorkflowCreation tests pass (create_workflow returns compiled graph, has all 4 nodes)
    - TestWorkflowStructure tests pass (sequential edges, conditional edges)
    - TestShouldProceedToEngineer tests pass (condition function returns correct values)
    - TestAgentStateCompatibility tests pass (TypedDict, Annotated with operator.add)
    - TestWorkflowInvocation tests pass (workflow invokes nodes, accumulates outputs)
    - TestWorkflowTypeHints tests pass (type annotations exist)

    These tests use mocking for LLM calls, so they don't require actual API keys.
  </action>
  <acceptance_criteria>
    - pytest command exits with code 0
    - Output contains at least 14 tests passed
    - No test failures or errors
    - TestWorkflowCreation: 4 tests pass
    - TestShouldProceedToEngineer: 3 tests pass
    - TestAgentStateCompatibility: 2 tests pass
    - TestWorkflowInvocation: 2 tests pass (with mock)
    - TestWorkflowTypeHints: 3 tests pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huahao/Desktop/code/claudecode/talk2code/backend && python -m pytest tests/integration/test_workflow.py -v</automated>
  </verify>
  <done>
    LangGraph workflow integration tests pass, confirming the StateGraph workflow executes correctly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → Source code | Tests import and execute source modules, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | test_llm_client.py | mitigate | Use Mock objects to avoid exposing API keys in tests (already implemented) |
| T-03-02 | Tampering | test_workflow.py | mitigate | Mock LLM responses to prevent external API manipulation during tests |
| T-03-03 | Spoofing | test_imports.py | accept | Test only verifies import paths, no authentication involved |
</threat_model>

<verification>
# Run all unit tests together to confirm TEST-01, TEST-02, TEST-03

cd /Users/huahao/Desktop/code/claudecode/talk2code/backend && python -m pytest tests/unit/ tests/integration/test_workflow.py -v --tb=short

Expected: All tests pass with exit code 0
</verification>

<success_criteria>
1. All import verification tests pass (17 tests in test_imports.py)
2. All LLM client tests pass (20+ tests in test_llm_client.py)
3. All workflow integration tests pass (14+ tests in test_workflow.py)
4. Total: 50+ tests passing
5. No deprecated import warnings
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-validation/03-01-SUMMARY.md` with:
- Test execution results (pytest output summary)
- Test count by category
- Any failures and resolutions
- Confirmation of TEST-01, TEST-02, TEST-03 coverage
</output>
