# Plan 01-02: 创建 Planner 节点

```yaml
---
wave: 1
depends_on: []
files_modified:
  - backend/agents/nodes.py
  - backend/agents/__init__.py
  - backend/prompts.py
autonomous: true
requirements_addressed: [PLN-01, PLN-02]
---
```

## Objective

删除 `researcher_node`、`product_manager_node`、`architect_node`，新建 `planner_node`。Planner 合并 3 个角色的职责，产出结构化 Plan JSON。

## Tasks

### Task 1: 编写 Planner Prompt

**Read first:** `backend/prompts.py` (了解现有 Prompt 格式)

**Action:** 在 `prompts.py` 中：

1. **删除**以下旧 import 和定义（标记为 Deprecated 的 4 套 system/user prompt）：
   - `RESEARCHER_SYSTEM_PROMPT`, `RESEARCHER_USER_PROMPT`
   - `PRODUCT_MANAGER_SYSTEM_PROMPT`, `PRODUCT_MANAGER_USER_PROMPT`
   - `ARCHITECT_SYSTEM_PROMPT`, `ARCHITECT_USER_PROMPT`
   - `ENGINEER_SYSTEM_PROMPT`, `ENGINEER_USER_PROMPT`
   - `AGENT_ORDER`, `AGENT_NAMES`, `AGENT_PROMPTS`

2. **新增** `PLANNER_PROMPT` 使用 `ChatPromptTemplate` 格式：

```python
PLANNER_PROMPT = ChatPromptTemplate.from_messages([
    SystemMessagePromptTemplate.from_template(
        "你是一位全栈产品架构师。请根据用户需求，产出一个结构化的开发计划。\n\n"
        "重要：这是一个纯前端应用，不需要后端服务器，所有数据使用 LocalStorage 存储。\n\n"
        "请严格按以下 JSON 格式输出，不要添加其他文字：\n"
        "{\n"
        '  "features": [{"name": "...", "description": "...", "priority": "high|medium|low"}],\n'
        '  "tech_stack": {"framework": "vanilla HTML/CSS/JS", "ui_library": "Tailwind CSS CDN", "storage": "LocalStorage"},\n'
        '  "data_model": [{"name": "...", "fields": [{"name": "...", "type": "..."}]}],\n'
        '  "file_structure": [{"file": "index.html", "responsibility": "..."}, ...],\n'
        '  "implementation_notes": ["...", "..."]\n'
        "}\n\n"
        "保持简洁，重点清晰。"
    ),
    HumanMessagePromptTemplate.from_template("用户需求：{requirement}\n\n请产出一个结构化的开发计划。")
])
```

3. **保留**以下内容（后续节点使用）：
   - `ENGINEER_PROMPT` → 改名为 `CODER_PROMPT`（在 Plan 02-01 中改造）
   - `CODE_EDIT_SYSTEM_PROMPT`, `CODE_EDIT_USER_PROMPT`（chat 修改用）
   - `generate_fallback_code()` 及相关 fallback 函数
   - `FALLBACK_RESPONSES` 中研究员/产品经理/架构师的模板 → 可以删除，只保留通用 fallback

**Acceptance criteria:**
- `prompts.py` 中无 `RESEARCHER_SYSTEM_PROMPT`, `PRODUCT_MANAGER_SYSTEM_PROMPT`, `ARCHITECT_SYSTEM_PROMPT` 定义
- `prompts.py` 包含 `PLANNER_PROMPT` 使用 `ChatPromptTemplate`
- `prompts.py` 仍包含 `generate_fallback_code()` 函数
- `prompts.py` 仍包含 `CODE_EDIT_SYSTEM_PROMPT` 和 `CODE_EDIT_USER_PROMPT`

### Task 2: 创建 planner_node 函数

**Read first:** `backend/agents/nodes.py`

**Action:** 在 `agents/nodes.py` 中：

1. **删除**以下旧节点函数：
   - `researcher_node()`
   - `product_manager_node()`
   - `architect_node()`
   - `compress_outputs()` (不再需要)
   - `create_agent_output()` (不再需要)

2. **新增** `planner_node(state: AgentState) -> Dict[str, Any]`：

```python
def planner_node(state: AgentState) -> Dict[str, Any]:
    """
    Planner 节点：合并研究员、产品经理、架构师职责，产出结构化 Plan
    """
    logger.info(f"[Planner] 开始分析需求：{state['requirement_id']}")

    try:
        client = get_client()
        messages = PLANNER_PROMPT.format_messages(requirement=state['requirement_content'])
        system_prompt = next((m.content for m in messages if m.type == 'system'), None)
        user_prompt = next((m.content for m in messages if m.type == 'human'), None)

        response = client.chat(
            prompt=user_prompt,
            system_prompt=system_prompt,
            use_memory=False,
            max_tokens=2000,
            timeout=45
        )

        if response.is_error:
            raise Exception(response.error)

        # 解析 JSON
        plan = extract_json_from_response(response.content)
        if not plan[0]:  # 解析失败
            # 生成默认 plan
            plan = {'features': [], 'tech_stack': {}, 'data_model': [], 'file_structure': [], 'implementation_notes': []}

        return {
            'plan': plan[1] if plan[0] else {},
            'current_step': 'planner_done',
            'dialogue_history': [{
                'role': 'agent',
                'name': 'Planner',
                'content': '已完成需求分析和架构设计',
                'status': 'completed'
            }],
            'metadata': {'planner_success': True}
        }

    except Exception as e:
        logger.error(f"[Planner] 执行失败：{e}")
        return {
            'plan': {},
            'current_step': 'planner_failed',
            'error': f"Planner 失败：{e}",
            'dialogue_history': [{
                'role': 'agent',
                'name': 'Planner',
                'content': f"分析失败，使用简化版：{state['requirement_content'][:50]}...",
                'status': 'failed'
            }],
            'metadata': {'planner_success': False}
        }
```

3. **保留** `engineer_node()` （改名为 `coder_node` 在 Plan 02-01 中做）和 `extract_json_from_response()` 辅助函数。

**Acceptance criteria:**
- `nodes.py` 中无 `researcher_node`, `product_manager_node`, `architect_node` 定义
- `nodes.py` 包含 `def planner_node(state:` 函数
- `nodes.py` 包含 `def coder_node(state:` 函数（原 engineer_node，暂时可保留原名）
- `nodes.py` 无 `compress_outputs` 和 `create_agent_output` 函数
- `python3 -c "from agents.nodes import planner_node; print('ok')"` 无报错

### Task 3: 更新 agents 模块导出

**Read first:** `backend/agents/__init__.py`

**Action:** 将导出从旧节点更新为新节点：

```python
# 旧：
from agents.nodes import (
    researcher_node,
    product_manager_node,
    architect_node,
    engineer_node
)
__all__ = ['AgentState', 'researcher_node', 'product_manager_node',
           'architect_node', 'engineer_node', 'create_workflow', 'get_workflow']

# 新：
from agents.nodes import planner_node, engineer_node
__all__ = ['AgentState', 'planner_node', 'engineer_node', 'create_workflow', 'get_workflow']
```

**Acceptance criteria:**
- `__init__.py` 中无 `researcher_node`, `product_manager_node`, `architect_node` 导出
- `__init__.py` 包含 `planner_node` 在 import 和 `__all__` 中
- `python3 -c "from agents import planner_node; print('ok')"` 无报错
