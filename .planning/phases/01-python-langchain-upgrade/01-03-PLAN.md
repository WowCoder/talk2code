---
phase: 01-python-langchain-upgrade
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - backend/tests/
  - backend/llm/client.py
  - backend/agents/workflow.py
autonomous: false
requirements:
  - PY-04
  - DEP-03
user_setup: []

must_haves:
  truths:
    - "Python version in venv is 3.11.11"
    - "Flask app starts without import errors"
    - "Unit tests pass with pytest"
    - "LLM client can be imported without errors"
    - "LangGraph workflow can be imported without errors"
  artifacts:
    - path: "backend/.venv/bin/python"
      provides: "Python 3.11.11 interpreter"
      version: "Python 3.11.11"
    - path: "backend/llm/client.py"
      provides: "LLM client module"
      imports: "langchain_core"
    - path: "backend/agents/workflow.py"
      provides: "LangGraph workflow"
      imports: "langgraph.graph"
  key_links:
    - from: "backend/.venv/bin/python"
      to: "backend/llm/client.py"
      via: "import langchain_core"
      pattern: "from langchain_core"
    - from: "backend/agents/workflow.py"
      to: "langgraph.graph"
      via: "from langgraph.graph import StateGraph"
      pattern: "langgraph\\.graph"
---

<objective>
Verify Python 3.11.11 environment and upgraded dependencies work correctly

Purpose: Confirm the upgrade is complete and functional before moving to API migration phase
Output: Verified environment, passing tests, working Flask app
</objective>

<execution_context>
@/Users/huahao/Desktop/code/claudecode/talk2code/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/PROJECT.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/ROADMAP.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/STATE.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/phases/01-python-langchain-upgrade/01-CONTEXT.md
@/Users/huahao/Desktop/code/claudecode/talk2code/backend/llm/client.py
@/Users/huahao/Desktop/code/claudecode/talk2code/backend/agents/workflow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Python version in virtual environment</name>
  <files>N/A (verification only)</files>
  <read_first>N/A</read_first>
  <action>
    Verify the virtual environment Python version:
    ```bash
    backend/.venv/bin/python --version
    ```
    
    Expected output: "Python 3.11.11"
    
    This verifies PY-04 (virtual environment Python version correct).
  </action>
  <verify>
    <automated>backend/.venv/bin/python --version outputs "Python 3.11.11"</automated>
  </verify>
  <done>
    Python version confirmed as 3.11.11 in virtual environment
  </done>
  <acceptance_criteria>
    - `backend/.venv/bin/python --version` outputs exactly "Python 3.11.11"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify core modules import without errors</name>
  <files>backend/llm/client.py, backend/agents/workflow.py, backend/agents/nodes.py</files>
  <read_first>backend/llm/client.py, backend/agents/workflow.py</read_first>
  <action>
    Test that key modules can be imported with the upgraded dependencies:
    ```bash
    cd backend
    source .venv/bin/activate
    python -c "from llm.client import LLMClient; print('LLM client OK')"
    python -c "from agents.workflow import create_workflow; print('Workflow OK')"
    python -c "from agents.nodes import researcher_node; print('Nodes OK')"
    ```
    
    Verify imports work with new langchain-core 1.x and langgraph 0.1.x APIs.
  </action>
  <verify>
    <automated>python -c "from llm.client import LLMClient" exits 0</automated>
  </verify>
  <done>
    All three core modules import successfully:
    - `from llm.client import LLMClient` exits 0 with output "LLM client OK"
    - `from agents.workflow import create_workflow` exits 0 with output "Workflow OK"
    - `from agents.nodes import researcher_node` exits 0 with output "Nodes OK"
    - No ImportError, ModuleNotFoundError, or AttributeError in stderr
  </done>
  <acceptance_criteria>
    - `python -c "from llm.client import LLMClient"` exits 0, stdout contains "LLM client OK"
    - `python -c "from agents.workflow import create_workflow"` exits 0, stdout contains "Workflow OK"
    - `python -c "from agents.nodes import researcher_node"` exits 0, stdout contains "Nodes OK"
    - stderr is empty or contains no "ImportError", "ModuleNotFoundError", or "AttributeError"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run existing unit tests</name>
  <files>backend/tests/</files>
  <read_first>backend/pytest.ini</read_first>
  <action>
    Run the existing unit test suite to verify compatibility:
    ```bash
    cd backend
    source .venv/bin/activate
    pytest tests/ -v
    ```
    
    Per D-05, run existing unit tests to verify upgrade compatibility.
    
    If tests fail, analyze whether failure is due to:
    - Test environment issue (fixable)
    - API breaking change (requires Phase 2 migration)
  </action>
  <verify>
    <automated>pytest tests/ exits 0 or shows expected failures</automated>
  </verify>
  <done>
    Unit tests executed, results documented
  </done>
  <acceptance_criteria>
    - `pytest tests/` command executes
    - Test output shows pass/fail summary
    - Any failures are documented with error messages
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Start Flask app and verify no startup errors</name>
  <files>N/A (verification only)</files>
  <read_first>backend/app.py</read_first>
  <action>
    Start Flask application and verify it launches without import errors:
    ```bash
    cd backend
    source .venv/bin/activate
    timeout 5 python app.py 2>&1 || true
    ```
    
    Use timeout to start app briefly and check for startup errors.
    Expected: Flask starts, shows "Running on http://..."
    Not expected: ImportError, ModuleNotFoundError, AttributeError
  </action>
  <verify>
    <automated>timeout 5 python app.py shows "Running on" or no ImportError</automated>
  </verify>
  <done>
    Flask app starts without import errors
  </done>
  <acceptance_criteria>
    - App startup output contains "Running on" or "Serving Flask app"
    - No "ImportError" or "ModuleNotFoundError" in stderr
    - No "AttributeError" related to langchain or langgraph
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 1 complete:
    - Python 3.11.11 environment set up (.python-version, backend/.venv)
    - Dependencies upgraded (langchain-core>=1.0.0, langgraph>=0.1.0)
    - Core modules import successfully
    - Unit tests executed
    - Flask app starts without errors
  </what-built>
  <how-to-verify>
    1. Verify Python version: `backend/.venv/bin/python --version` should show "Python 3.11.11"
    2. Verify installed packages: `source backend/.venv/bin/activate && pip list | grep langchain` should show langchain-core>=1.0.0
    3. Check .python-version file exists in project root with content "3.11.11"
    4. Review pytest output for any failures
  </how-to-verify>
  <resume-signal>Type "approved" if Phase 1 is complete, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → application code | Tests verify application behavior without modifying it |
| Flask app → system | App runs in isolated venv, limited system access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of Service | Flask app startup | mitigate | Timeout used (5s) to prevent hanging; venv isolates from system |
| T-01-07 | Elevation of Privilege | venv isolation | accept | venv provides process-level isolation, sufficient for development |
</threat_model>

<verification>
- Python version is 3.11.11 in venv
- All core modules import without errors
- Unit tests pass (or failures documented)
- Flask app starts without import errors
</verification>

<success_criteria>
- PY-04 verified: Virtual environment uses Python 3.11.11
- DEP-03 verified: Installed packages work without import errors
- Phase 1 goal achieved: Python 3.11.11 environment + upgraded dependencies
- Ready to proceed to Phase 2 (LangChain API migration)
</success_criteria>

<output>
After completion, create `.planning/phases/01-python-langchain-upgrade/01-verification-PLAN-03-SUMMARY.md`
</output>
