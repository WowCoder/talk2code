---
phase: 01-python-langchain-upgrade
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/requirements.txt
  - backend/.venv/lib/python*/site-packages/
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
user_setup: []

must_haves:
  truths:
    - "requirements.txt specifies langchain-core>=1.0.0"
    - "requirements.txt specifies langgraph>=0.1.0"
    - "pip install completes without errors"
    - "pip list shows langchain-core version >= 1.0.0"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "Updated dependency specifications"
      contains: "langchain-core>=1.0.0"
    - path: "backend/.venv/lib/python3.11/site-packages/langchain_core/"
      provides: "Installed langchain-core package"
      exists: true
  key_links:
    - from: "backend/requirements.txt"
      to: "pip install -r requirements.txt"
      via: "dependency installation"
      pattern: "langchain-core.*1\\.0"
    - from: "backend/.venv/bin/pip"
      to: "installed packages"
      via: "package installation"
      pattern: "pip list"
---

<objective>
Update requirements.txt and install upgraded dependencies (langchain-core 1.x, langgraph 0.1.x)

Purpose: Complete the dependency upgrade phase with proper version pinning and conflict resolution
Output: Updated requirements.txt, installed packages in virtual environment
</objective>

<execution_context>
@/Users/huahao/Desktop/code/claudecode/talk2code/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/PROJECT.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/ROADMAP.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/STATE.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/phases/01-python-langchain-upgrade/01-CONTEXT.md
@/Users/huahao/Desktop/code/claudecode/talk2code/backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backup current requirements.txt</name>
  <files>backend/requirements.txt.backup</files>
  <read_first>backend/requirements.txt</read_first>
  <action>
    Create backup of current requirements.txt before modification (D-08):
    ```bash
    cp backend/requirements.txt backend/requirements.txt.backup
    ```
    
    This enables rollback if needed (D-09).
  </action>
  <verify>
    <automated>cat backend/requirements.txt.backup contains "langchain-core>=0.1.0"</automated>
  </verify>
  <done>
    Backup created at backend/requirements.txt.backup
  </done>
  <acceptance_criteria>
    - File backend/requirements.txt.backup exists
    - File content matches original requirements.txt
    - Contains "langchain-core>=0.1.0" and "langgraph>=0.0.40"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update requirements.txt with new versions</name>
  <files>backend/requirements.txt</files>
  <read_first>backend/requirements.txt</read_first>
  <action>
    Update backend/requirements.txt with these exact version changes:
    
    FROM:
    ```
    langchain-core>=0.1.0
    langgraph>=0.0.40
    ```
    
    TO:
    ```
    langchain-core>=1.0.0
    langgraph>=0.1.0
    ```
    
    Keep all other dependencies unchanged.
    
    Per D-03 (progressive upgrade), we update langchain-core and langgraph in place.
  </action>
  <verify>
    <automated>grep "langchain-core>=1.0.0" backend/requirements.txt returns match</automated>
  </verify>
  <done>
    requirements.txt updated with langchain-core>=1.0.0 and langgraph>=0.1.0
  </done>
  <acceptance_criteria>
    - File contains exactly "langchain-core>=1.0.0"
    - File contains exactly "langgraph>=0.1.0"
    - All other dependencies remain unchanged (flask, sqlalchemy, pytest, etc.)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Install updated dependencies</name>
  <files>backend/.venv/lib/python*/site-packages/</files>
  <read_first>backend/requirements.txt</read_first>
  <action>
    Activate virtual environment and install dependencies:
    ```bash
    cd backend
    source .venv/bin/activate
    pip install --upgrade pip
    pip install -r requirements.txt
    ```
    
    Per D-04, run冒烟测试 after installation to verify no conflicts.
  </action>
  <verify>
    <automated>pip install -r requirements.txt exits 0</automated>
  </verify>
  <done>
    All dependencies installed successfully without conflicts
  </done>
  <acceptance_criteria>
    - `pip install -r requirements.txt` completes with exit code 0
    - No ERROR or CONFLICT messages in pip output
    - Final output shows "Successfully installed" or "Requirement already satisfied"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Verify installed package versions</name>
  <files>N/A (verification only)</files>
  <read_first>N/A</read_first>
  <action>
    Verify installed versions match requirements:
    ```bash
    source backend/.venv/bin/activate
    pip list | grep -E "langchain-core|langgraph"
    ```
    
    Expected output should show:
    - langchain-core >= 1.0.0
    - langgraph >= 0.1.0
  </action>
  <verify>
    <automated>pip list | grep "langchain-core" shows version >= 1.0.0</automated>
  </verify>
  <done>
    Verified langchain-core >= 1.0.0 and langgraph >= 0.1.0 are installed
  </done>
  <acceptance_criteria>
    - `pip list` output contains "langchain-core" with version >= 1.0.0
    - `pip list` output contains "langgraph" with version >= 0.1.0
    - No old versions (0.x) of langchain-core present
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| venv → system packages | Virtual environment isolates project from system Python packages |
| requirements.txt → pip install | Dependency file controls what gets installed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | requirements.txt | mitigate | Backup created (requirements.txt.backup); git tracks changes |
| T-01-04 | Supply Chain | pip install | accept | Using official PyPI; versions pinned with >= constraints; standard Python practice |
| T-01-05 | Information Disclosure | .venv/site-packages | accept | Contains installed packages only; excluded from git via .gitignore |
</threat_model>

<verification>
- `pip install -r requirements.txt` exits 0
- `pip list | grep langchain-core` shows version >= 1.0.0
- `pip list | grep langgraph` shows version >= 0.1.0
- No import errors when running Flask app
</verification>

<success_criteria>
- requirements.txt updated with langchain-core>=1.0.0 and langgraph>=0.1.0
- All dependencies installed without conflicts
- Verified package versions via pip list
- Backup file exists for rollback
</success_criteria>

<output>
After completion, create `.planning/phases/01-python-langchain-upgrade/01-dependency-upgrade-PLAN-02-SUMMARY.md`
</output>
