---
phase: 01-python-langchain-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .python-version
  - backend/.venv/
autonomous: true
requirements:
  - PY-01
  - PY-02
  - PY-03
  - PY-04
user_setup: []

must_haves:
  truths:
    - "pyenv knows Python 3.11.11 is installed"
    - ".python-version file exists and contains 3.11.11"
    - "Virtual environment exists at backend/.venv"
    - "Virtual environment uses Python 3.11.11"
  artifacts:
    - path: ".python-version"
      provides: "pyenv version specification"
      contains: "3.11.11"
    - path: "backend/.venv/bin/python"
      provides: "Virtual environment Python interpreter"
      version: "Python 3.11.11"
  key_links:
    - from: ".python-version"
      to: "pyenv local"
      via: "pyenv reads file on shell cd"
      pattern: "cd backend activates 3.11.11"
    - from: "backend/.venv"
      to: "Python 3.11.11"
      via: "python -m venv .venv"
      pattern: "source .venv/bin/activate"
---

<objective>
Set up Python 3.11.11 environment using pyenv and create virtual environment

Purpose: Establish the foundation for LangChain upgrade by ensuring correct Python version
Output: .python-version file, backend/.venv virtual environment
</objective>

<execution_context>
@/Users/huahao/Desktop/code/claudecode/talk2code/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/PROJECT.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/ROADMAP.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/STATE.md
@/Users/huahao/Desktop/code/claudecode/talk2code/.planning/phases/01-python-langchain-upgrade/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pre-upgrade git checkpoint (D-07)</name>
  <files>git repository</files>
  <read_first>N/A</read_first>
  <action>
    Per D-07 "升级前使用 git 保存当前状态", create a git checkpoint before any modifications:
    ```bash
    git add -A
    git commit -m "Pre-upgrade snapshot: before Python 3.11.11 and LangChain upgrade"
    ```
    
    This provides a clean rollback point per D-09 if the upgrade encounters issues.
    
    If there are no uncommitted changes, still create an empty commit to mark the checkpoint:
    ```bash
    git commit --allow-empty -m "Pre-upgrade checkpoint: before Python 3.11.11 upgrade"
    ```
  </action>
  <verify>
    <automated>git log -1 --oneline contains "Pre-upgrade" in commit message</automated>
  </verify>
  <done>
    Git checkpoint commit created with message containing "Pre-upgrade"
  </done>
  <acceptance_criteria>
    - `git log -1 --format="%s"` outputs a message containing "Pre-upgrade"
    - Commit created before any phase modifications begin
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify Python 3.11.11 is installed via pyenv</name>
  <files>N/A (verification only)</files>
  <read_first>N/A</read_first>
  <action>
    Run pyenv commands to verify Python 3.11.11 availability:
    1. `pyenv versions` - list all installed versions
    2. `pyenv prefix 3.11.11` - verify 3.11.11 is installed
    
    If 3.11.11 is NOT installed, run: `pyenv install 3.11.11`
    
    Per STATE.md, pyenv already has Python 3.11.11 installed (D-02).
  </action>
  <verify>
    <automated>pyenv prefix 3.11.11 returns a valid path</automated>
  </verify>
  <done>
    Python 3.11.11 is confirmed installed via pyenv
  </done>
  <acceptance_criteria>
    - `pyenv versions` output contains "3.11.11"
    - `pyenv prefix 3.11.11` returns a path like /Users/huahao/.pyenv/versions/3.11.11
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create .python-version file in project root</name>
  <files>.python-version</files>
  <read_first>N/A</read_first>
  <action>
    Create .python-version file with exact content:
    ```
    3.11.11
    ```
    
    This file tells pyenv which Python version to use when cd-ing into this directory (D-01).
    
    Then run `pyenv local 3.11.11` to verify the file is recognized.
  </action>
  <verify>
    <automated>cat .python-version outputs "3.11.11"</automated>
  </verify>
  <done>
    .python-version file created with content "3.11.11"
  </done>
  <acceptance_criteria>
    - File .python-version exists in project root
    - File content is exactly "3.11.11" (with optional newline)
    - `pyenv local` reads the version correctly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Create virtual environment at backend/.venv</name>
  <files>backend/.venv/</files>
  <read_first>N/A</read_first>
  <action>
    Navigate to backend directory and create virtual environment:
    ```bash
    cd backend
    python3.11 -m venv .venv
    ```
    
    Per D-02, use Python built-in venv module (not conda).
    Per D-01, use .venv as directory name.
    
    Verify the venv was created correctly by checking Python version.
  </action>
  <verify>
    <automated>backend/.venv/bin/python --version outputs "Python 3.11.11"</automated>
  </verify>
  <done>
    Virtual environment created at backend/.venv using Python 3.11.11
  </done>
  <acceptance_criteria>
    - Directory backend/.venv exists
    - File backend/.venv/bin/python exists and is executable
    - `backend/.venv/bin/python --version` outputs "Python 3.11.11"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyenv → system Python | pyenv manages Python versions isolated from system Python |
| venv → global site-packages | Virtual environment isolates project dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | .python-version | mitigate | File is simple text, no execution risk; git track for audit |
| T-01-02 | Information Disclosure | .venv directory | accept | Contains no secrets, only installed packages; .gitignore should exclude .venv |
</threat_model>

<verification>
- `pyenv prefix 3.11.11` returns valid path
- `cat .python-version` outputs "3.11.11"
- `backend/.venv/bin/python --version` outputs "Python 3.11.11"
</verification>

<success_criteria>
- Python 3.11.11 is installed and accessible via pyenv
- .python-version file exists in project root
- Virtual environment exists at backend/.venv with correct Python version
- Pre-upgrade git checkpoint created (D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/01-python-langchain-upgrade/01-python-env-PLAN-01-SUMMARY.md`
</output>
