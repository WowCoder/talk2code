任务需求：{requirement}

以下是可用的技能列表，每个技能描述了什么情况下应该使用它：

{menu}

请选择对当前任务有帮助的技能序号，返回 JSON 数组。选择原则：
- 任何有 UI 界面的任务都应考虑 color 和 typography
- 只需排除明确不相关的技能（如无表单的任务排除 accessibility）
- 不限制数量，只要相关就选上
- 如果都不确定，返回 []